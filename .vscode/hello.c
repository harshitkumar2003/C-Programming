#include<stdio.h>
int main()
{
    // -------------------------------------------------------------
    // Printing hello world the basic first program
    // printf("Hello World");
    // -------------------------------------------------------------

    // -------------------------------------------------------------
    // Printing sum of two variable 
    int a = 2;
    int b = 1;
    // printf("Enter the value of a:");
    // scanf("%d",&a);
    // printf("Enter the value of b:");
    // scanf("%d",&b);
    printf("\nThe sum of a and b is: %d", a+b);
    printf("\nThe product of a and b is: %d", a-b);
    printf("\nThe multiolication of a and b is: %d", a*b);
    printf("\nThe divisor of a and b is: %d", a/b);
    printf("\nThe remainder of a and b is: %d", a%b);
    // -------------------------------------------------------------
return 0;
}